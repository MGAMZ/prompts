# 注释守则

## 通用规则

### 必须

- 注释、PR 描述、ISSUE 描述只写结论，不写思考过程和调试备忘。
- 代码与文档只描述它是什么。
- 源文件头部、函数头部的注释块只说明主要功能，不描述实现逻辑。

### 禁止

- 描述函数的执行逻辑，代码本身能表达的内容不需要注释再说一次。
- 写过程性或排除性注释，如"本模块不负责 XX，由 XX 负责"。
- 描述没做什么，只描述做了什么。
- 使用``被包裹的内容``，统一使用`被包裹的内容`。
- 在`被包裹的内容`中使用引号。

### 例外

以下情况允许保留解释性语句：
- 非显而易见，极具迷惑性的的约束、边界条件。
- 性能、安全、兼容性原因。
- 省略会让读者误判改动边界，如"对外 API 不变"。
- 用户明确要求。
- 安全/兼容等硬性约束。

## 用户文档（README、安装指引、示例说明）

README 面向不熟悉本仓库的新用户，只保留完成首次使用所需的信息。

### 必须

- 首屏包含：这是什么、能做什么、怎么跑、最小示例。
- 先写安装/运行/最小示例，再写概念解释。
- 每一个句子都要通过检验：删掉它，读者还能完成他带着这份文档要办的实际需求吗？如果能，就删除。

### 禁止

- 写现状担忧叙事。
- 出现作者机器专属绝对路径与个人标识，包括文档、代码默认值、示例配置、测试。无合理默认值的参数设为必填，文档默认值列写"必填"。示例配置用占位符或仓库相对路径，驱动脚本经 CLI 参数接收路径。conda 环境名、用户名、个人盘符均不属于可发布信息。
- 解释"为什么这样能跑、为什么用这个入口而不用那个"。反直觉选择本身不需要辩护，原理属于代码 docstring 或设计文档。
- 标题与小节名带实现标签。
- 首段超过三句，仓库内部沿革与脚手架遗留进入首屏。
- 引用非仓库分发处的内容。
- 引用其他源文件中的具体行。

## 冲突时的优先级

正确性/安全性 > 用户可执行性 > 简洁性 > 风格统一
